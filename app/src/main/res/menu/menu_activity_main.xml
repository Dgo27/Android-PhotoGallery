<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Search coordinates by address -->
    <item
        android:id="@+id/action_search_address"
        android:icon="@drawable/ic_action_search"
        android:title="@string/action_search_address"
        app:showAsAction="always"/>

    <!-- Get current user position if possible -->
    <item android:id="@+id/action_current_position"
        android:icon="@drawable/ic_my_location"
        android:title="@string/action_get_current_position"
        app:showAsAction="always"/>

</menu>